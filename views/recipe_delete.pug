extends layout

block content
  main.delete
    h1 Delete #{recipe.name}
    p Are you sure you want to delete this recipe?
    form(method="POST")
      .form-input
        input#recipeid(type="hidden" name="recipeid" value=recipe._id)
      button(type="submit") Delete